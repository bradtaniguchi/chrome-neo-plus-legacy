/**
 * The public base url to make APOD http requests to.
 *
 * This should be used until we have a private api instance, which will
 * then call this URL with a private api_key for for the client.
 */
export const APOD_API_URL = 'https://api.nasa.gov/planetary/apod';
